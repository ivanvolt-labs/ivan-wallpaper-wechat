<view class="back-arrow">
  <image class="back-image" src="{{icons.back}}" bindtap="goBack"></image>
  <view style="color:#ccc8c8;font-size:36rpx;">|</view>
  <image class="home-image" src="{{icons.home}}" bindtap="goHome"></image>
</view>